import React from 'react';

interface IProps {}

const WalletInformation: React.FC<IProps> = (props: IProps) => {
  console.log(props);
  return <>It's Interactive Form tab!</>;
};

export default WalletInformation;
